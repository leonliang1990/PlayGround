---
description: 关键词驱动的 Git 版本控制流程
alwaysApply: true
---

# Git 关键词自动流程

适用范围：当前仓库内的所有会话。

- 保留三个关键词（中英文等价）：
  - **迭代 / iterate**
  - **发版 / publish**
  - **回滚 / rollback**

- 当用户说 **“迭代 / iterate”**：
  - 仅执行版本控制动作，不做功能开发。
  - 只做本地提交：`git add -A`、commit。
  - 默认不 push，不改版本号，不打 tag。

- 当用户说 **“发版 / publish”**：
  - 仅执行版本控制动作，不做功能开发。
  - 版本号默认做 patch 升级（`x.y.z -> x.y.(z+1)`）；若用户指定版本号，按用户指定。
  - 版本号优先更新：
    - `extension/manifest.json`
    - 若不存在则尝试 `ig-following-sort/extension/manifest.json`
  - 执行 `git add -A`、commit、push。
  - 创建并推送 tag：`v<version>`。
  - 默认不创建 GitHub Release（除非用户明确要求）。

- 当用户说 **“回滚 / rollback”**：
  - 仅执行回滚动作，不做功能开发。
  - 若用户指定目标版本（如 `v0.3.0`），优先回滚到该 tag。
  - 若用户未指定，默认回滚到上一个版本 tag。
  - 回滚默认仅在本地执行，不 push。
  - 只有用户明确说“回滚并同步”时，才 push 到远程。
  - 回滚优先使用“可追踪”的方式（例如基于 revert 产生新 commit）。
  - 执行前先说明将回滚到哪个版本/tag，执行后汇报结果（本地/已同步）。

- 安全规则：
  - 不提交隐私或导出数据（如 `IG_INFO/`、凭据文件、`.env`）。
  - 若工作区存在可疑敏感文件，先提醒用户再继续。

- 沟通风格：
  - 执行前用 1 句话复述将要做的动作。
  - 执行后汇报：新版本号、commit hash、是否已 push、tag 名称。
